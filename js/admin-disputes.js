document.addEventListener('DOMContentLoaded', loadDisputes);
async function loadDisputes() { const container = document.getElementById('disputesList'); container.innerHTML = '<p class="text-center">Chargement des litiges...</p>'; try { const response = await fetch('/api/v1/disputes.php'); const result = await response.json(); if (!response.ok) throw new Error(result.error || 'Chargement impossible'); const disputes = result.data || []; container.innerHTML = disputes.length ? disputes.map(renderDispute).join('') : '<p class="text-center">Aucun litige.</p>'; } catch (error) { container.innerHTML = `<p class="text-center">${escapeHtml(error.message)}</p>`; } }
function renderDispute(dispute) { const active = ['open', 'under_review'].includes(dispute.status); return `<article class="list-item"><div class="item-info"><div class="item-name">Litige #${dispute.id} · Commande #${dispute.order_id}</div><div class="item-details">${escapeHtml(dispute.client_name)} vs ${escapeHtml(dispute.artisan_name)} · ${escapeHtml(dispute.reason)}</div><p>${escapeHtml(dispute.description)}</p><div class="item-details">Statut : ${escapeHtml(dispute.status)}</div>${dispute.resolution ? `<p>Résolution : ${escapeHtml(dispute.resolution)}</p>` : ''}</div>${active ? `<div class="item-actions"><button class="btn btn-primary btn-small" onclick="resolveDispute(${dispute.id}, 'resolve')">Résoudre</button><button class="btn btn-outline btn-small" onclick="resolveDispute(${dispute.id}, 'reject')">Rejeter</button></div>` : ''}</article>`; }
async function resolveDispute(disputeId, action) { const resolution = window.prompt('Saisissez la décision administrative :'); if (!resolution) return; try { const response = await fetch('/api/v1/disputes.php', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ disputeId, action, resolution }) }); const result = await response.json(); if (!response.ok) throw new Error(result.error || 'Mise à jour impossible'); showNotification(result.message, 'success'); await loadDisputes(); } catch (error) { showNotification(error.message, 'error'); } }
function escapeHtml(value) { return String(value ?? '').replace(/[&<>'"]/g, character => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#039;', '"': '&quot;' }[character])); }
