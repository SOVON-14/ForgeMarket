document.addEventListener('DOMContentLoaded', () => { const form = document.getElementById('disputeForm'); const orderId = new URLSearchParams(window.location.search).get('order'); if (!orderId) { showNotification('Commande manquante.', 'error'); form.querySelector('button[type="submit"]').disabled = true; return; } form.addEventListener('submit', async event => { event.preventDefault(); const formData = new FormData(form); const submitButton = form.querySelector('button[type="submit"]'); submitButton.disabled = true; try { const response = await fetch('/api/v1/disputes.php', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ action: 'open', orderId: Number(orderId), reason: formData.get('reason'), description: formData.get('description') }) }); const result = await response.json(); if (!response.ok) throw new Error(result.error || 'Impossible d’ouvrir le litige.'); showNotification(result.message, 'success'); setTimeout(() => { window.location.href = 'orders.html'; }, 800); } catch (error) { showNotification(error.message, 'error'); submitButton.disabled = false; } }); });
