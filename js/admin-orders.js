document.addEventListener('DOMContentLoaded', async () => { const container = document.getElementById('ordersList'); try { const response = await fetch('/api/v1/admin-orders.php'); const result = await response.json(); if (!response.ok) throw new Error(result.error || 'Commandes indisponibles'); container.innerHTML = result.data.length ? result.data.map(order => `<article class="list-item"><div class="item-info"><div class="item-name">Commande #${order.id} · ${escapeHtml(order.title)}</div><div class="item-details">${escapeHtml(order.client)} → ${escapeHtml(order.artisan)}</div></div><div class="item-actions"><span class="badge">${escapeHtml(order.status)}</span><span class="item-details">${Number(order.amount).toLocaleString('fr-FR')} FCFA</span></div></article>`).join('') : '<p class="text-center">Aucune commande.</p>'; } catch (error) { container.innerHTML = `<p class="text-center">${escapeHtml(error.message)}</p>`; } }); function escapeHtml(value) { return String(value ?? '').replace(/[&<>'"]/g, character => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#039;', '"': '&quot;' }[character])); }
