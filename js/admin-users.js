document.addEventListener('DOMContentLoaded', async () => { const container = document.getElementById('usersList'); try { const response = await fetch('/api/v1/admin-users.php'); const result = await response.json(); if (!response.ok) throw new Error(result.error || 'Utilisateurs indisponibles'); container.innerHTML = result.data.length ? result.data.map(user => `<article class="list-item"><div class="item-info"><div class="item-name">${escapeHtml(user.first_name)} ${escapeHtml(user.last_name)}</div><div class="item-details">${escapeHtml(user.email)} · ${escapeHtml(user.phone)}</div></div><span class="badge">${escapeHtml(user.role)}</span></article>`).join('') : '<p class="text-center">Aucun utilisateur.</p>'; } catch (error) { container.innerHTML = `<p class="text-center">${escapeHtml(error.message)}</p>`; } }); function escapeHtml(value) { return String(value ?? '').replace(/[&<>'"]/g, character => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#039;', '"': '&quot;' }[character])); }
